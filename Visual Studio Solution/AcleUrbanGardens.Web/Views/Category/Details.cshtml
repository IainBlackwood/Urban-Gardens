@model AcleUrbanGardens.Domain.Category

@{
    ViewBag.Title = Model.Name;
    ViewBag.CreateProductMessage = "Create a new " + Model.Name + " product";
}

<div>
    <h2>@ViewBag.Title Detail</h2>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CreatedBy)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CreatedBy)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CreateDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CreateDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.UpdatedBy)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UpdatedBy)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.UpdateDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UpdateDate)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index") |
    @Html.ActionLink("Delete", "Delete", "Category", new { id = Model.Id }, null)
</p>

<div class="row">



</div>

<div>
    <h3>The Category @Model.Name has the following Sub-Categories</h3>
    @{
        if (Model.Children.Count > 0)
        {
            foreach (var category in Model.Children)
            {
                <div>
                    <h4>
                        @Html.ActionLink(category.Name, "Details", "Category", new { Id = category.Id }, null)
                    </h4>
                    <p>Products attached to the Sub-Category</p>
                    <ul>
                        @{
                            if (category.Products.Count() > 0)
                            {
                                foreach (var product in category.Products)
                                {
                                    <li>@Html.ActionLink(product.Name, "Details", "Product", new { Id = product.Id }, null)</li>
                                }
                            }
                            else
                            {
                                <li>There are no Products in the @category.Name sub-category</li>
                            }

                        }
                        
                    </ul>
                </div>
            }
        }
        else
        {
            <div>
                <label>There are no sub categories</label>
            </div>
        }
    }
</div>


<div>
    <h3>Products that are included in the @Model.Name Category</h3>
    @if (Model.Products.Count > 0)
    {
        <ul>
            @foreach(var product in Model.Products)
            {
                <li>@Html.ActionLink(@product.Name, "Details", "Product", new { Id = @product.Id }, null)</li>
            }
        </ul>
    }
    else
    {
        <ul>
            <li>There are no Products in the @Model.Name category.</li>
        </ul>
    }
    @Html.ActionLink((string)ViewBag.CreateProductMessage, "Create", "Product", new { categoryId = Model.Id }, null)
</div>

